from setuptools import setup

setup(
    name='roboto-build',
    version='0.0.1',
    author='Google',
    packages=['scripts', 'tests'],
    license='LICENSE.txt',
    description='Build chain to make v3 roboto fonts',
)
